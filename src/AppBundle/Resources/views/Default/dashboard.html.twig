{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
<h1>Projekty</h1>
{% for project in projects %}
  <h3><a href="{{ path('entries', {'project_id': project.id}) }}">{{ project.client.name }}: {{ project.name }}</a></h3>
{% endfor %}
{% endblock %}